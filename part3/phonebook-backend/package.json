{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "author": "Ani",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../../part2/phonebook/exercise && npm run build && cp -r dist ../../../part3/phonebook-backend",
    "deploy:full": "npm run build:ui && cd ../../ && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "express": "^5.2.1",
    "mongoose": "^9.3.3",
    "morgan": "^1.10.1"
  }
}
